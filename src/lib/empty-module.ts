    // Browser stub for node builtins optional dep branches probe for (wasmoon's loader does await import('module')). Aliased via turbopack.resolveAlias.
const emptyModule = {};
export default emptyModule;
