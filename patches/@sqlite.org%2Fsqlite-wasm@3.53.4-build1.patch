diff --git a/dist/index.mjs b/dist/index.mjs
index 5e6e43ba2a02c3c7a9d71aabb8134a929ffc94ca..417383f79af51e60480b0bc6fdb8bd36423716b4 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -16251,8 +16251,15 @@ async function sqlite3InitModule$1(moduleArg = {}) {
 				*/
 				getAssociatedPath(sah) {
 					sah.read(this.#apBody, { at: 0 });
+					const pathBytes = this.#apBody.findIndex((v) => 0 === v);
+					const path = pathBytes ? textDecoder.decode(this.#apBody.subarray(0, pathBytes)) : "";
+					const keep = !!path && sah.getSize() > HEADER_OFFSET_DATA;
 					const flags = this.#dvBody.getUint32(HEADER_OFFSET_FLAGS);
 					if (this.#apBody[0] && (flags & capi.SQLITE_OPEN_DELETEONCLOSE || (flags & PERSISTENT_FILE_TYPES) === 0)) {
+						if (keep && !(flags & capi.SQLITE_OPEN_DELETEONCLOSE)) {
+							warn(`Keeping ${path} despite unexpected flags ${flags.toString(16)}`, "because it holds data.");
+							return path;
+						}
 						warn(`Removing file with unexpected flags ${flags.toString(16)}`, this.#apBody);
 						this.setAssociatedPath(sah, "", 0);
 						return "";
@@ -16261,9 +16268,11 @@ async function sqlite3InitModule$1(moduleArg = {}) {
 					sah.read(fileDigest, { at: HEADER_OFFSET_DIGEST });
 					const compDigest = this.computeDigest(this.#apBody, flags);
 					if (fileDigest.every((v, i) => v === compDigest[i])) {
-						const pathBytes = this.#apBody.findIndex((v) => 0 === v);
 						if (0 === pathBytes) sah.truncate(HEADER_OFFSET_DATA);
-						return pathBytes ? textDecoder.decode(this.#apBody.subarray(0, pathBytes)) : "";
+						return path;
+					} else if (keep) {
+						warn(`Keeping ${path} despite a bad digest because it holds data.`);
+						return path;
 					} else {
 						warn("Disassociating file with bad digest.");
 						this.setAssociatedPath(sah, "", 0);
diff --git a/dist/sqlite3-worker1.mjs b/dist/sqlite3-worker1.mjs
index 55836559c280d5263be6258ba404bb3870081d86..8d41c9e7101e1df95bdbfc7a2bf8817f41240190 100644
--- a/dist/sqlite3-worker1.mjs
+++ b/dist/sqlite3-worker1.mjs
@@ -16326,8 +16326,15 @@ async function sqlite3InitModule(moduleArg = {}) {
 				*/
 				getAssociatedPath(sah) {
 					sah.read(this.#apBody, { at: 0 });
+					const pathBytes = this.#apBody.findIndex((v) => 0 === v);
+					const path = pathBytes ? textDecoder.decode(this.#apBody.subarray(0, pathBytes)) : "";
+					const keep = !!path && sah.getSize() > HEADER_OFFSET_DATA;
 					const flags = this.#dvBody.getUint32(HEADER_OFFSET_FLAGS);
 					if (this.#apBody[0] && (flags & capi.SQLITE_OPEN_DELETEONCLOSE || (flags & PERSISTENT_FILE_TYPES) === 0)) {
+						if (keep && !(flags & capi.SQLITE_OPEN_DELETEONCLOSE)) {
+							warn(`Keeping ${path} despite unexpected flags ${flags.toString(16)}`, "because it holds data.");
+							return path;
+						}
 						warn(`Removing file with unexpected flags ${flags.toString(16)}`, this.#apBody);
 						this.setAssociatedPath(sah, "", 0);
 						return "";
@@ -16336,9 +16343,11 @@ async function sqlite3InitModule(moduleArg = {}) {
 					sah.read(fileDigest, { at: HEADER_OFFSET_DIGEST });
 					const compDigest = this.computeDigest(this.#apBody, flags);
 					if (fileDigest.every((v, i) => v === compDigest[i])) {
-						const pathBytes = this.#apBody.findIndex((v) => 0 === v);
 						if (0 === pathBytes) sah.truncate(HEADER_OFFSET_DATA);
-						return pathBytes ? textDecoder.decode(this.#apBody.subarray(0, pathBytes)) : "";
+						return path;
+					} else if (keep) {
+						warn(`Keeping ${path} despite a bad digest because it holds data.`);
+						return path;
 					} else {
 						warn("Disassociating file with bad digest.");
 						this.setAssociatedPath(sah, "", 0);
