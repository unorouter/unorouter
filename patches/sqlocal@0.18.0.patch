diff --git a/dist/client.d.ts b/dist/client.d.ts
index cd50b15f408e463ff7298398dd96ecd2144d35f0..b532d03fbd233a11a3daacc08c85432f66d76640 100644
--- a/dist/client.d.ts
+++ b/dist/client.d.ts
@@ -1,5 +1,5 @@
 import type { CallbackUserFunction, QueryKey, RawResultData, Sqlite3Method, ScalarUserFunction, Statement, DatabaseInfo, ClientConfig, StatementInput, Transaction, DatabasePath, AggregateUserFunction, ReactiveQuery, SqlTag, TransactionHandle, WindowUserFunction, UserFunction } from './types.js';
-import type { BatchMessage, BroadcastMessage, DeleteMessage, DestroyMessage, ExportMessage, FunctionMessage, GetInfoMessage, ImportMessage, OmitQueryKey, OutputMessage, QueryMessage, TransactionMessage, WorkerProxy } from './messages.js';
+import type { BatchMessage, BroadcastMessage, DeleteMessage, DestroyMessage, ExportMessage, FunctionMessage, GetInfoMessage, ImportMessage, OmitQueryKey, OutputMessage, PurgeOrphansMessage, QueryMessage, TransactionMessage, WorkerProxy } from './messages.js';
 import { SQLocalProcessor } from './processor.js';
 /**
  * This class is your entry point for connecting to and
@@ -21,10 +21,11 @@ export declare class SQLocal {
     protected proxy: WorkerProxy;
     protected reinitChannel: BroadcastChannel;
     protected effectsChannel?: BroadcastChannel;
+    protected unloadListener?: () => void;
     constructor(databasePath: DatabasePath);
     constructor(config: ClientConfig);
     protected processMessageEvent: (event: OutputMessage | MessageEvent<OutputMessage>) => void;
-    protected createQuery: (message: OmitQueryKey<QueryMessage | BatchMessage | TransactionMessage | FunctionMessage | GetInfoMessage | ImportMessage | ExportMessage | DeleteMessage | DestroyMessage>) => Promise<OutputMessage>;
+    protected createQuery: (message: OmitQueryKey<QueryMessage | BatchMessage | TransactionMessage | FunctionMessage | GetInfoMessage | ImportMessage | ExportMessage | DeleteMessage | PurgeOrphansMessage | DestroyMessage>) => Promise<OutputMessage>;
     protected broadcast: (message: BroadcastMessage) => void;
     /** @internal */
     exec: (sql: string, params: unknown[], method?: Sqlite3Method, transactionKey?: QueryKey) => Promise<RawResultData>;
@@ -98,6 +99,20 @@ export declare class SQLocal {
      * @see {@link https://sqlocal.dev/api/deletedatabasefile}
      */
     deleteDatabaseFile: (beforeUnlock?: () => void | Promise<void>, destroy?: boolean) => Promise<void>;
+    /**
+     * Remove every OPFS entry that belongs to this database (the main file,
+     * its `-journal` / `-wal` / `-shm` sidecars, and any orphaned
+     * `backup-*--<fileName>` files left behind by interrupted `export()`
+     * calls). Returns the list of file names that were removed.
+     *
+     * Unlike {@link deleteDatabaseFile}, this does not require the database
+     * to be in a usable state, which makes it a useful recovery path for
+     * users whose OPFS quota has been eaten by orphans the regular SQLite
+     * shutdown sequence never managed to clean up.
+     *
+     * For non-OPFS drivers this is a no-op and returns an empty array.
+     */
+    purgeOrphans: () => Promise<string[]>;
     /**
      * Disconnect this SQLocal client from the database and terminate
      * its worker thread.
diff --git a/dist/client.js b/dist/client.js
index c51880cf9589b91754e7645c4f376ddff62c4d91..8a20f61621ebfca675690c04f9f6c9dcf77c10e1 100644
--- a/dist/client.js
+++ b/dist/client.js
@@ -83,6 +83,12 @@ export class SQLocal {
             writable: true,
             value: void 0
         });
+        Object.defineProperty(this, "unloadListener", {
+            enumerable: true,
+            configurable: true,
+            writable: true,
+            value: void 0
+        });
         Object.defineProperty(this, "processMessageEvent", {
             enumerable: true,
             configurable: true,
@@ -95,6 +101,7 @@ export class SQLocal {
                     case 'data':
                     case 'buffer':
                     case 'info':
+                    case 'purgeOrphans':
                     case 'error':
                         if (message.queryKey && queries.has(message.queryKey)) {
                             const [resolve, reject] = queries.get(message.queryKey);
@@ -653,6 +660,50 @@ export class SQLocal {
                 }
             }
         });
+        /**
+         * Remove every OPFS entry that belongs to this database (the main file,
+         * its `-journal` / `-wal` / `-shm` sidecars, and any orphaned
+         * `backup-*--<fileName>` files left behind by interrupted `export()`
+         * calls). Returns the list of file names that were removed.
+         *
+         * Unlike {@link deleteDatabaseFile}, this does not require the database
+         * to be in a usable state, which makes it a useful recovery path for
+         * users whose OPFS quota has been eaten by orphans the regular SQLite
+         * shutdown sequence never managed to clean up.
+         *
+         * For non-OPFS drivers this is a no-op and returns an empty array.
+         */
+        Object.defineProperty(this, "purgeOrphans", {
+            enumerable: true,
+            configurable: true,
+            writable: true,
+            value: async () => {
+                const removed = await mutationLock({
+                    mode: 'exclusive',
+                    bypass: false,
+                    key: getDatabaseKey(this.config.databasePath, this.clientKey),
+                }, async () => {
+                    try {
+                        this.broadcast({
+                            type: 'close',
+                            clientKey: this.clientKey,
+                        });
+                        const message = await this.createQuery({ type: 'purgeOrphans' });
+                        const removed = message.type === 'purgeOrphans' ? message.removed : [];
+                        this.broadcast({
+                            type: 'reinit',
+                            clientKey: this.clientKey,
+                            reason: 'delete',
+                        });
+                        return removed;
+                    }
+                    finally {
+                        this.bypassMutationLock = false;
+                    }
+                });
+                return removed;
+            }
+        });
         /**
          * Disconnect this SQLocal client from the database and terminate
          * its worker thread.
@@ -669,6 +720,12 @@ export class SQLocal {
                     this.processor.removeEventListener('message', this.processMessageEvent);
                     this.processor.terminate();
                 }
+                if (this.unloadListener &&
+                    typeof globalThis.removeEventListener === 'function') {
+                    globalThis.removeEventListener('pagehide', this.unloadListener);
+                    globalThis.removeEventListener('beforeunload', this.unloadListener);
+                    this.unloadListener = undefined;
+                }
                 this.queriesInProgress.clear();
                 this.userCallbacks.clear();
                 this.reinitChannel.close();
@@ -703,7 +760,7 @@ export class SQLocal {
             }
         });
         const clientConfig = typeof config === 'string' ? { databasePath: config } : config;
-        const { onInit, onConnect, processor, ...commonConfig } = clientConfig;
+        const { onInit, onConnect, processor, releaseOnUnload: _releaseOnUnload, ...commonConfig } = clientConfig;
         const { databasePath } = commonConfig;
         this.config = clientConfig;
         this.clientKey = getQueryKey();
@@ -750,6 +807,25 @@ export class SQLocal {
                 onInitStatements: onInit?.(sqlTag) ?? [],
             },
         });
+        // Auto-release the OPFS sync-access-handle on page unload. Without
+        // this, an unclean tab close (crash, OOM, forced reload) can leave
+        // the SAH locked, and Chromium/Brave will not reclaim the underlying
+        // file-system bytes until the entire origin's storage is evicted.
+        // `pagehide` fires for both bfcache freeze and real navigation;
+        // `beforeunload` is a fallback for browsers that do not dispatch
+        // `pagehide` reliably.
+        if (clientConfig.releaseOnUnload === true &&
+            typeof globalThis.addEventListener === 'function') {
+            this.unloadListener = () => {
+                void this.destroy(true).catch(() => { });
+            };
+            globalThis.addEventListener('pagehide', this.unloadListener, {
+                once: true,
+            });
+            globalThis.addEventListener('beforeunload', this.unloadListener, {
+                once: true,
+            });
+        }
     }
 }
 _a = Symbol.dispose, _b = Symbol.asyncDispose;
diff --git a/dist/drivers/sqlite-memory-driver.d.ts b/dist/drivers/sqlite-memory-driver.d.ts
index 9eef5221b3e3205e462b115916a3a2b8d9f5fe5f..482c91277d586fac64c26a8598e01c201ea9f109 100644
--- a/dist/drivers/sqlite-memory-driver.d.ts
+++ b/dist/drivers/sqlite-memory-driver.d.ts
@@ -25,6 +25,7 @@ export declare class SQLiteMemoryDriver implements SQLocalDriver {
         data: ArrayBuffer | Uint8Array<ArrayBuffer>;
     }>;
     clear(): Promise<void>;
+    purgeOrphans(): Promise<string[]>;
     destroy(): Promise<void>;
     protected getFlags(config: DriverConfig): string;
     protected execOnDb(db: Sqlite3Db, statement: DriverStatement): RawResultData;
diff --git a/dist/drivers/sqlite-memory-driver.js b/dist/drivers/sqlite-memory-driver.js
index de02edca2f3381499ff51e2cb904cb078617c6c1..ed0be5c941b8635ab626f85e89e2d07cd67cf3c1 100644
--- a/dist/drivers/sqlite-memory-driver.js
+++ b/dist/drivers/sqlite-memory-driver.js
@@ -181,6 +181,10 @@ export class SQLiteMemoryDriver {
         };
     }
     async clear() { }
+    async purgeOrphans() {
+        // In-memory driver has no on-disk artifacts. Nothing to purge.
+        return [];
+    }
     async destroy() {
         this.closeDb();
         this.pointers.forEach((pointer) => this.sqlite3?.wasm.dealloc(pointer));
diff --git a/dist/drivers/sqlite-opfs-driver.d.ts b/dist/drivers/sqlite-opfs-driver.d.ts
index 22489de5c844195edca7d6e9533f68f620f61089..e4bf3030618d42e35c58f1d672eb9c54b48693d7 100644
--- a/dist/drivers/sqlite-opfs-driver.d.ts
+++ b/dist/drivers/sqlite-opfs-driver.d.ts
@@ -15,5 +15,16 @@ export declare class SQLiteOpfsDriver extends SQLiteMemoryDriver implements SQLo
         data: ArrayBuffer | Uint8Array<ArrayBuffer>;
     }>;
     clear(): Promise<void>;
+    /**
+     * Remove every OPFS entry in the database's directory that belongs to this
+     * database (the main file, its `-journal` / `-wal` / `-shm` sidecars, and
+     * any orphaned `backup-*--<fileName>` files from interrupted `export()`
+     * calls). Returns the list of entry names that were removed. Unlike
+     * `clear()`, this does not depend on the runtime SQLite state and can be
+     * called even when the driver is not initialized, which makes it a useful
+     * recovery path for users whose OPFS quota is being eaten by orphans the
+     * regular SQLite shutdown sequence never managed to clean up.
+     */
+    purgeOrphans(): Promise<string[]>;
     destroy(): Promise<void>;
 }
diff --git a/dist/drivers/sqlite-opfs-driver.js b/dist/drivers/sqlite-opfs-driver.js
index 5cab774d23558e6d585e5d22f930d408ffbbf3f2..7d9f971947a851acd67b300a0b5e85664c666f98 100644
--- a/dist/drivers/sqlite-opfs-driver.js
+++ b/dist/drivers/sqlite-opfs-driver.js
@@ -73,8 +73,19 @@ export class SQLiteOpfsDriver extends SQLiteMemoryDriver {
         await this.destroy();
         const { getDirectoryHandle, fileName, tempFileNames } = parseDatabasePath(this.config.databasePath);
         const dirHandle = await getDirectoryHandle();
-        const fileNames = [fileName, ...tempFileNames];
-        await Promise.all(fileNames.map(async (name) => {
+        const fileNames = new Set([fileName, ...tempFileNames]);
+        // Also sweep any leftover `backup-*--<fileName>` files. These are
+        // created by `export()` (`VACUUM INTO ...`) and removed on the happy
+        // path, but a crash or tab close during export can leave them behind.
+        // Without this sweep they accumulate, contributing to OPFS bloat that
+        // is opaque to consumers.
+        const backupSuffix = `--${fileName}`;
+        for await (const entryName of dirHandle.keys()) {
+            if (entryName.startsWith('backup-') && entryName.endsWith(backupSuffix)) {
+                fileNames.add(entryName);
+            }
+        }
+        await Promise.all([...fileNames].map(async (name) => {
             return dirHandle.removeEntry(name).catch((err) => {
                 if (!(err instanceof DOMException && err.name === 'NotFoundError')) {
                     throw err;
@@ -82,6 +93,43 @@ export class SQLiteOpfsDriver extends SQLiteMemoryDriver {
             });
         }));
     }
+    /**
+     * Remove every OPFS entry in the database's directory that belongs to this
+     * database (the main file, its `-journal` / `-wal` / `-shm` sidecars, and
+     * any orphaned `backup-*--<fileName>` files from interrupted `export()`
+     * calls). Returns the list of entry names that were removed. Unlike
+     * `clear()`, this does not depend on the runtime SQLite state and can be
+     * called even when the driver is not initialized, which makes it a useful
+     * recovery path for users whose OPFS quota is being eaten by orphans the
+     * regular SQLite shutdown sequence never managed to clean up.
+     */
+    async purgeOrphans() {
+        if (!this.config?.databasePath)
+            throw new Error('Driver not initialized');
+        await this.destroy();
+        const { getDirectoryHandle, fileName, tempFileNames } = parseDatabasePath(this.config.databasePath);
+        const dirHandle = await getDirectoryHandle();
+        const removed = [];
+        const sidecarSet = new Set(tempFileNames);
+        const backupSuffix = `--${fileName}`;
+        for await (const entryName of dirHandle.keys()) {
+            const isMain = entryName === fileName;
+            const isSidecar = sidecarSet.has(entryName);
+            const isBackup = entryName.startsWith('backup-') && entryName.endsWith(backupSuffix);
+            if (!isMain && !isSidecar && !isBackup)
+                continue;
+            try {
+                await dirHandle.removeEntry(entryName);
+                removed.push(entryName);
+            }
+            catch (err) {
+                if (!(err instanceof DOMException && err.name === 'NotFoundError')) {
+                    throw err;
+                }
+            }
+        }
+        return removed;
+    }
     async destroy() {
         this.closeDb();
         this.writeCallbacks.clear();
diff --git a/dist/messages.d.ts b/dist/messages.d.ts
index 733da264a482cc9db3093c6323dc04f049398709..9cd77d2a99b8aae90a28e7c18564a49df929ad67 100644
--- a/dist/messages.d.ts
+++ b/dist/messages.d.ts
@@ -2,7 +2,7 @@ import type { ConnectReason, DatabaseInfo, ProcessorConfig, QueryKey, Sqlite3Met
 export type Message = InputMessage | OutputMessage;
 export type OmitQueryKey<T> = T extends Message ? Omit<T, 'queryKey'> : never;
 export type WorkerProxy = (typeof globalThis | ProxyHandler<Worker>) & Record<string, (...args: any) => any>;
-export type InputMessage = QueryMessage | BatchMessage | TransactionMessage | FunctionMessage | ConfigMessage | GetInfoMessage | ImportMessage | ExportMessage | DeleteMessage | DestroyMessage;
+export type InputMessage = QueryMessage | BatchMessage | TransactionMessage | FunctionMessage | ConfigMessage | GetInfoMessage | ImportMessage | ExportMessage | DeleteMessage | PurgeOrphansMessage | DestroyMessage;
 export type QueryMessage = {
     type: 'query';
     queryKey: QueryKey;
@@ -55,12 +55,16 @@ export type DeleteMessage = {
     queryKey: QueryKey;
     destroy: boolean;
 };
+export type PurgeOrphansMessage = {
+    type: 'purgeOrphans';
+    queryKey: QueryKey;
+};
 export type DestroyMessage = {
     type: 'destroy';
     queryKey: QueryKey;
     skipOptimize: boolean;
 };
-export type OutputMessage = SuccessMessage | ErrorMessage | DataMessage | BufferMessage | CallbackMessage | InfoMessage | EventMessage;
+export type OutputMessage = SuccessMessage | ErrorMessage | DataMessage | BufferMessage | CallbackMessage | InfoMessage | PurgeOrphansResponseMessage | EventMessage;
 export type SuccessMessage = {
     type: 'success';
     queryKey: QueryKey;
@@ -95,6 +99,11 @@ export type InfoMessage = {
     queryKey: QueryKey;
     info: DatabaseInfo;
 };
+export type PurgeOrphansResponseMessage = {
+    type: 'purgeOrphans';
+    queryKey: QueryKey;
+    removed: string[];
+};
 export type EventMessage = {
     type: 'event';
     event: 'connect';
diff --git a/dist/processor.d.ts b/dist/processor.d.ts
index 985566c23dadc40fd87180567cccbbc00552667c..df5ea88ac8671f9a742a312418f34a03a52acd75 100644
--- a/dist/processor.d.ts
+++ b/dist/processor.d.ts
@@ -1,5 +1,5 @@
 import type { ProcessorConfig, UserFunction, QueryKey, ConnectReason, SQLocalDriver } from './types.js';
-import type { BatchMessage, ConfigMessage, DeleteMessage, DestroyMessage, ExportMessage, FunctionMessage, GetInfoMessage, ImportMessage, InputMessage, OutputMessage, QueryMessage, TransactionMessage, WorkerProxy } from './messages.js';
+import type { BatchMessage, ConfigMessage, DeleteMessage, DestroyMessage, ExportMessage, FunctionMessage, GetInfoMessage, ImportMessage, InputMessage, OutputMessage, PurgeOrphansMessage, QueryMessage, TransactionMessage, WorkerProxy } from './messages.js';
 import { type Mutex } from './lib/create-mutex.js';
 import { type DebouncedFunction } from './lib/debounce.js';
 /**
@@ -42,5 +42,6 @@ export declare class SQLocalProcessor {
     protected importDb: (message: ImportMessage) => Promise<void>;
     protected exportDb: (message: ExportMessage) => Promise<void>;
     protected deleteDb: (message: DeleteMessage) => Promise<void>;
+    protected purgeOrphans: (message: PurgeOrphansMessage) => Promise<void>;
     protected destroy: (message?: DestroyMessage) => Promise<void>;
 }
diff --git a/dist/processor.js b/dist/processor.js
index 960d1f8c9b7f2ffacbe5f97a63ceb45bef0050ec..1a9c8eb4aa76bb7f68c30a1f9ab52dc0c31e5563 100644
--- a/dist/processor.js
+++ b/dist/processor.js
@@ -174,6 +174,9 @@ export class SQLocalProcessor {
                     case 'delete':
                         this.deleteDb(message);
                         break;
+                    case 'purgeOrphans':
+                        this.purgeOrphans(message);
+                        break;
                     case 'destroy':
                         this.destroy(message);
                         break;
@@ -500,6 +503,34 @@ export class SQLocalProcessor {
                 }
             }
         });
+        Object.defineProperty(this, "purgeOrphans", {
+            enumerable: true,
+            configurable: true,
+            writable: true,
+            value: async (message) => {
+                const { queryKey } = message;
+                let removed = [];
+                try {
+                    removed = await this.driver.purgeOrphans();
+                }
+                catch (error) {
+                    this.emitMessage({
+                        type: 'error',
+                        error,
+                        queryKey,
+                    });
+                    return;
+                }
+                finally {
+                    await this.init('delete');
+                }
+                this.emitMessage({
+                    type: 'purgeOrphans',
+                    queryKey,
+                    removed,
+                });
+            }
+        });
         Object.defineProperty(this, "destroy", {
             enumerable: true,
             configurable: true,
diff --git a/dist/types.d.ts b/dist/types.d.ts
index a1953566f1bed525b284ac8d9c64589de60a8974..d66b845da9bbbcb26994f5f844116a93b68dbbd6 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -54,6 +54,7 @@ export interface SQLocalDriver {
         data: ArrayBuffer | Uint8Array<ArrayBuffer>;
     }>;
     clear: () => Promise<void>;
+    purgeOrphans: () => Promise<string[]>;
     destroy: () => Promise<void>;
 }
 export type DriverConfig = {
@@ -75,6 +76,16 @@ export type ClientConfig = {
     reactive?: boolean;
     readOnly?: boolean;
     verbose?: boolean;
+    /**
+     * When `true`, the client listens for `pagehide` and `beforeunload` events
+     * and calls `destroy()` automatically. Releases the OPFS sync-access-handle
+     * lock so the browser can reclaim file-system bytes (otherwise an unclean
+     * tab close can leave the SAH allocated, contributing to phantom OPFS
+     * usage that does not appear in `getDirectory()` enumeration).
+     *
+     * @default false
+     */
+    releaseOnUnload?: boolean;
     onInit?: (sql: SqlTag) => void | Statement[];
     onConnect?: (reason: ConnectReason) => void;
     processor?: SQLocalProcessor | Worker;
