diff --git a/dist/react/runtimes/RemoteThreadListThreadListRuntimeCore.js b/dist/react/runtimes/RemoteThreadListThreadListRuntimeCore.js
--- a/dist/react/runtimes/RemoteThreadListThreadListRuntimeCore.js
+++ b/dist/react/runtimes/RemoteThreadListThreadListRuntimeCore.js
@@ -107,15 +107,10 @@
                 Fragment),
         }));
         this.__internal_setOptions(options);
-        const startThreadId = options.threadId ?? options.initialThreadId;
-        if (startThreadId) {
-            this.switchToThread(startThreadId);
-        }
-        else {
-            this.switchToNewThread();
-        }
+        this.switchToNewThread();
     }
     useProvider;
+    _initialThreadLoaded = false;
     __internal_setOptions(options) {
         if (this._options === options)
             return;
@@ -129,6 +124,11 @@
     }
     __internal_load() {
         this.getLoadThreadsPromise(); // begin loading on initial bind
+        const startThreadId = this._options.threadId ?? this._options.initialThreadId;
+        if (!this._initialThreadLoaded && startThreadId) {
+            this._initialThreadLoaded = true;
+            this.switchToThread(startThreadId);
+        }
     }
     get isLoading() {
         return this._state.value.isLoading;
diff --git a/src/react/runtimes/RemoteThreadListThreadListRuntimeCore.tsx b/src/react/runtimes/RemoteThreadListThreadListRuntimeCore.tsx
--- a/src/react/runtimes/RemoteThreadListThreadListRuntimeCore.tsx
+++ b/src/react/runtimes/RemoteThreadListThreadListRuntimeCore.tsx
@@ -149,15 +149,11 @@
         Fragment) as ComponentType<PropsWithChildren>,
     }));
     this.__internal_setOptions(options);
-    const startThreadId = options.threadId ?? options.initialThreadId;
-    if (startThreadId) {
-      this.switchToThread(startThreadId);
-    } else {
-      this.switchToNewThread();
-    }
+    this.switchToNewThread();
   }

   private useProvider;
+  private _initialThreadLoaded = false;

   public __internal_setOptions(options: RemoteThreadListOptions) {
     if (this._options === options) return;
@@ -175,6 +171,11 @@

   public __internal_load() {
     this.getLoadThreadsPromise(); // begin loading on initial bind
+    const startThreadId = this._options.threadId ?? this._options.initialThreadId;
+    if (!this._initialThreadLoaded && startThreadId) {
+      this._initialThreadLoaded = true;
+      this.switchToThread(startThreadId);
+    }
   }

   public get isLoading() {
