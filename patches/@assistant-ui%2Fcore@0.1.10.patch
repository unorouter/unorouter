diff --git a/node_modules/@assistant-ui/core/.bun-tag-868bb137e011bc3b b/.bun-tag-868bb137e011bc3b
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/react/runtimes/RemoteThreadListThreadListRuntimeCore.js b/dist/react/runtimes/RemoteThreadListThreadListRuntimeCore.js
index b5dc1006b1e0856abdd867a73f6a7f3e54c232a2..0805b2f2027fd32bae158e55f911c24fa9aeab5f 100644
--- a/dist/react/runtimes/RemoteThreadListThreadListRuntimeCore.js
+++ b/dist/react/runtimes/RemoteThreadListThreadListRuntimeCore.js
@@ -102,6 +102,7 @@ export class RemoteThreadListThreadListRuntimeCore extends BaseSubscribable {
         this.switchToNewThread();
     }
     useProvider;
+    _initialThreadLoaded = false;
     __internal_setOptions(options) {
         if (this._options === options)
             return;
@@ -115,6 +116,10 @@ export class RemoteThreadListThreadListRuntimeCore extends BaseSubscribable {
     }
     __internal_load() {
         this.getLoadThreadsPromise(); // begin loading on initial bind
+        if (!this._initialThreadLoaded && this._options.initialThreadId) {
+            this._initialThreadLoaded = true;
+            this.switchToThread(this._options.initialThreadId);
+        }
     }
     get isLoading() {
         return this._state.value.isLoading;
diff --git a/dist/react/runtimes/useRemoteThreadListRuntime.js b/dist/react/runtimes/useRemoteThreadListRuntime.js
index 8f71beecc83925f1a521d935eeae93f6bb8b1a02..a45eafea66c9cd34e56d694c25b5f7c39bb90e41 100644
--- a/dist/react/runtimes/useRemoteThreadListRuntime.js
+++ b/dist/react/runtimes/useRemoteThreadListRuntime.js
@@ -24,12 +24,15 @@ const useRemoteThreadListRuntimeImpl = (options) => {
 export const useRemoteThreadListRuntime = (options) => {
     const runtimeHookRef = useRef(options.runtimeHook);
     runtimeHookRef.current = options.runtimeHook;
+    // initialThreadId is only consumed by the constructor; capture once via ref
+    const initialThreadIdRef = useRef(options.initialThreadId);
     const stableRuntimeHook = useCallback(() => {
         return runtimeHookRef.current();
     }, []);
     const stableOptions = useMemo(() => ({
         adapter: options.adapter,
         allowNesting: options.allowNesting,
+        initialThreadId: initialThreadIdRef.current,
         runtimeHook: stableRuntimeHook,
     }), [options.adapter, options.allowNesting, stableRuntimeHook]);
     const aui = useAui();
diff --git a/dist/runtimes/remote-thread-list/types.d.ts b/dist/runtimes/remote-thread-list/types.d.ts
index 5c0641f634a9082096a65e378ed9a6d3be7be52b..4fa8aa87a194c09e61cc40b764e1aca0ea0b9a91 100644
--- a/dist/runtimes/remote-thread-list/types.d.ts
+++ b/dist/runtimes/remote-thread-list/types.d.ts
@@ -28,6 +28,12 @@ export type RemoteThreadListAdapter = {
 export type RemoteThreadListOptions = {
     runtimeHook: () => AssistantRuntime;
     adapter: RemoteThreadListAdapter;
+    /**
+     * When provided, the runtime starts on this thread instead of creating a
+     * new empty thread. Useful for URL-based routing (e.g. `/chat/[threadId]`)
+     * where the initial thread is known at mount time.
+     */
+    initialThreadId?: string | undefined;
     /**
      * When true, if this runtime is used inside another RemoteThreadListRuntime,
      * it becomes a no-op and simply calls the runtimeHook directly.
diff --git a/src/react/runtimes/RemoteThreadListThreadListRuntimeCore.tsx b/src/react/runtimes/RemoteThreadListThreadListRuntimeCore.tsx
index b71017f38d3d3184ceeffab6aef8b86980dba1ee..a93bd2e271668e3f50ff08717055a822f45f89cd 100644
--- a/src/react/runtimes/RemoteThreadListThreadListRuntimeCore.tsx
+++ b/src/react/runtimes/RemoteThreadListThreadListRuntimeCore.tsx
@@ -145,6 +145,7 @@ export class RemoteThreadListThreadListRuntimeCore
   }
 
   private useProvider;
+  private _initialThreadLoaded = false;
 
   public __internal_setOptions(options: RemoteThreadListOptions) {
     if (this._options === options) return;
@@ -162,6 +163,10 @@ export class RemoteThreadListThreadListRuntimeCore
 
   public __internal_load() {
     this.getLoadThreadsPromise(); // begin loading on initial bind
+    if (!this._initialThreadLoaded && this._options.initialThreadId) {
+      this._initialThreadLoaded = true;
+      this.switchToThread(this._options.initialThreadId);
+    }
   }
 
   public get isLoading() {
diff --git a/src/react/runtimes/useRemoteThreadListRuntime.ts b/src/react/runtimes/useRemoteThreadListRuntime.ts
index d757ee0a8c95405a6d0c30c8b51832841a8d2099..521e0beb99de89c49ae5189bf8371d6e49d2b6de 100644
--- a/src/react/runtimes/useRemoteThreadListRuntime.ts
+++ b/src/react/runtimes/useRemoteThreadListRuntime.ts
@@ -43,6 +43,9 @@ export const useRemoteThreadListRuntime = (
   const runtimeHookRef = useRef(options.runtimeHook);
   runtimeHookRef.current = options.runtimeHook;
 
+  // initialThreadId is only consumed by the constructor; capture once via ref
+  const initialThreadIdRef = useRef(options.initialThreadId);
+
   const stableRuntimeHook = useCallback(() => {
     return runtimeHookRef.current();
   }, []);
@@ -51,6 +54,7 @@ export const useRemoteThreadListRuntime = (
     () => ({
       adapter: options.adapter,
       allowNesting: options.allowNesting,
+      initialThreadId: initialThreadIdRef.current,
       runtimeHook: stableRuntimeHook,
     }),
     [options.adapter, options.allowNesting, stableRuntimeHook],
diff --git a/src/runtimes/remote-thread-list/types.ts b/src/runtimes/remote-thread-list/types.ts
index dcd687a7a215ac71c9ad764969524539b8a828b0..a74fd2b46034e94ca7fffed52f0c62db99bad982 100644
--- a/src/runtimes/remote-thread-list/types.ts
+++ b/src/runtimes/remote-thread-list/types.ts
@@ -39,6 +39,13 @@ export type RemoteThreadListOptions = {
   runtimeHook: () => AssistantRuntime;
   adapter: RemoteThreadListAdapter;
 
+  /**
+   * When provided, the runtime starts on this thread instead of creating a
+   * new empty thread. Useful for URL-based routing (e.g. `/chat/[threadId]`)
+   * where the initial thread is known at mount time.
+   */
+  initialThreadId?: string | undefined;
+
   /**
    * When true, if this runtime is used inside another RemoteThreadListRuntime,
    * it becomes a no-op and simply calls the runtimeHook directly.
